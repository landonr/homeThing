- name: ESPHome Builder
  # You may pin to the exact commit or the version.
  # uses: esphome/build-action@94054922590c1e1f63a6438cf5904138b515e5ee
  uses: esphome/build-action@v1.5.2
  with:
    # YAML file to use
    yaml_file: lilygo.yaml
    # Version of ESPHome to build
    version: # optional, default is latest
